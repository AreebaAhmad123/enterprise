<!DOCTYPE html>
<html>
  <head>
    <title>Consulting Scheduler</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
      @keyframes fadeIn {
        from { opacity: 0; transform: translateY(10px); }
        to { opacity: 1; transform: translateY(0); }
      }
      .animate-fade-in { animation: fadeIn 0.5s ease-out forwards; }
      .input-focus {
        transition: border-color 0.3s ease, box-shadow 0.3s ease;
      }
      .input-focus:focus {
        border-color: #000000;
        box-shadow: 0 0 0 3px rgba(0, 0, 0, 0.1);
        outline: none;
      }
      .btn-hover {
        transition: background-color 0.3s ease, transform 0.2s ease;
      }
      .btn-hover:hover { transform: translateY(-2px); }
    </style>
  </head>
  <body class="bg-gray-100 text-gray-800 font-sans min-h-screen flex flex-col">
    <nav class="bg-black shadow-md">
      <div class="container mx-auto px-4 py-3 flex justify-between items-center">
        <%= link_to '📅 Consulting Scheduler', root_path, class: 'text-white font-semibold text-xl' %>
        <div class="flex items-center space-x-4">
          <% if current_user %>
            <div class="flex items-center space-x-4">
              <%= link_to 'Profile', profile_path, class: 'text-white hover:text-gray-200 transition' %>
              <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: 'text-white hover:text-red-200 transition' %>
            </div>
          <% else %>
            <%= link_to 'Login', new_user_session_path, class: 'text-white hover:text-gray-200 transition' %>
            <%= link_to 'Sign Up', new_user_registration_path, class: 'text-white hover:text-gray-200 transition' %>
          <% end %>
        </div>
      </div>
    </nav>
    <main class="flex-grow container mx-auto px-4 py-6">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <%= yield %>
    </main>
    <footer class="bg-black border-t text-center text-sm text-white py-4">
      © <%= Time.now.year %> Consulting Scheduler. All rights reserved.
    </footer>
  </body>
</html>