<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Log In - Consulting Session Scheduler</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .animate-fade-in {
      animation: fadeIn 0.5s ease-out forwards;
    }
    .input-focus {
      transition: border-color 0.3s ease, box-shadow 0.3s ease;
    }
    .input-focus:focus {
      border-color: #000000;
      box-shadow: 0 0 0 3px rgba(0, 0, 0, 0.1);
      outline: none;
    }
    .btn-hover {
      transition: background-color 0.3s ease, transform 0.2s ease;
    }
    .btn-hover:hover {
      transform: translateY(-2px);
    }
  </style>
</head>
<body class="min-h-screen bg-gray-50 flex items-center justify-center p-4">
  <div class="bg-white p-8 rounded-2xl shadow-lg w-full max-w-md animate-fade-in mx-auto">
    <h2 class="text-3xl font-extrabold text-center text-gray-900 mb-6 flex items-center justify-center">
      <span class="mr-2">🔐</span> Log In
    </h2>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "space-y-6" }) do |f| %>
      <div>
        <%= f.label :email, class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full px-4 py-3 border border-gray-300 rounded-lg bg-gray-50 text-gray-900 input-focus", placeholder: " " %>
      </div>

      <div>
        <%= f.label :password, class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.password_field :password, autocomplete: "current-password", class: "w-full px-4 py-3 border border-gray-300 rounded-lg bg-gray-50 text-gray-900 input-focus", placeholder: " " %>
      </div>

      <div class="flex items-center">
        <%= f.check_box :remember_me, class: "h-4 w-4 text-black focus:ring-black border-gray-300 rounded" %>
        <%= f.label :remember_me, class: "ml-2 text-sm text-gray-600" %>
      </div>

      <div>
        <%= f.submit "Log In", class: "w-full bg-black text-white py-3 rounded-lg font-semibold btn-hover hover:bg-gray-800" %>
      </div>
    <% end %>

    <div class="mt-6 space-y-3 text-center">
      <p class="text-sm text-gray-600">Don't have an account? 
        <%= link_to "Sign up", new_registration_path(resource_name), class: "text-black font-medium hover:underline" %>
      </p>
      <p class="text-sm text-gray-600">
        <%= link_to "Forgot your password?", new_password_path(resource_name), class: "text-black font-medium hover:underline" %>
      </p>
    </div>
  </div>
</body>
</html>